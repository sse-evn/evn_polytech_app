import 'package:flutter/material.dart';

class ENotaryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('eNotary')),
      body: Center(child: Text('eNotary Screen')),
    );
  }
}
