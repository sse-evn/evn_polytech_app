import 'package:flutter/material.dart';

class EgovQRScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('eGov QR')),
      body: Center(child: Text('eGov QR Screen')),
    );
  }
}
