import 'package:flutter/material.dart';

class SocialWalletScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Social Wallet')),
      body: Center(child: Text('Social Wallet Screen')),
    );
  }
}
