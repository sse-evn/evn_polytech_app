import 'package:flutter/material.dart';

class VideoPSCScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Video PSC')),
      body: Center(child: Text('Video PSC Screen')),
    );
  }
}
