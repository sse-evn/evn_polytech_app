import 'package:flutter/material.dart';

class EDensauyqScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('eDensauyq')),
      body: Center(child: Text('eDensauyq Screen')),
    );
  }
}
