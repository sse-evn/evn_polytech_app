import 'package:flutter/material.dart';

class ESalyqScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('eSalyq')),
      body: Center(child: Text('eSalyq Screen')),
    );
  }
}
